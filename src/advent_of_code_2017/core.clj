(ns advent-of-code-2017.core
  (:gen-class)
  (:require ;[clojure.spec.alpha :as s]
            ;[clojure.spec.gen.alpha :as gen]
            ;[clojure.spec.test.alpha :as gen]
            [advent-of-code-2017.day1.1 :as day1-1]))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
